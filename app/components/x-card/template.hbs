<div class="card-content">
    {{#if title}}
        <h2 class="md-title">{{title}}</h2>
    {{/if}}
    {{yield}}

</div>
{{#if attrs.onsubmit}}
    {{#paper-dialog-actions class="layout-row"}}
        <span flex></span>
        {{#if attrs.onsecondbutton}}
            {{#paper-button primary=true onClick=(action "onsecondbutton")}}{{secondButtonText}}{{/paper-button}}
        {{/if}}
        {{#paper-button raised=true primary=true disabled=buttonDisabled onClick=(action "onsubmit" this)}}{{submitButtonText}}{{/paper-button}}
    {{/paper-dialog-actions}}
{{/if}}