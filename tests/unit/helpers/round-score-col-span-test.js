import { roundScoreColSpan } from 'contest-director/helpers/round-score-col-span';
import { module, test } from 'qunit';

module('Unit | Helper | round score col span');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = roundScoreColSpan([42]);
  assert.ok(result);
});
